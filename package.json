{
  "name": "camino",
  "version": "0.0.1",
  "description": "Roadmap your life",
  "dependencies": {
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^5.0.1",
    "electron-prebuilt": "0.30.3"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . Camino --platform=darwin --arch=x64 --version=0.30.3 --ignore=node_modules/electron --icon=Icon.icns",
    "start": "electron ."
  }
}
